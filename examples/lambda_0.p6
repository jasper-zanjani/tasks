#!/usr/bin/env perl6
use v6;

my $l = * + 12

# On compilation, the "whatever star" which is a placeholder for the argument, will be converted to a lambda expression. Source: [Python to Perl 6 - nutshell](https://docs.perl6.org/language/py-nutshell)
